# Backend runtime dependencies, pinned to the versions this project was built
# and tested against locally. Unpinned installs drift; a Docker rebuild six
# months from now would silently pull a different LangChain and break.
#
# torch is NOT listed here on purpose — the Dockerfile installs the CPU-only
# wheel from PyTorch's own index first. The default PyPI torch wheel bundles
# CUDA and is ~2.5GB; the CPU wheel is ~200MB and the free HF Space has no GPU.

fastapi==0.141.1
uvicorn==0.52.3
pydantic==2.13.4
starlette==1.6.0

langgraph==1.2.11
langchain==1.3.15
langchain-core==1.5.5
langchain-classic==1.0.8
langchain-groq==1.1.3
groq==0.37.1

psycopg2-binary==2.9.12
pgvector==0.5.0

sentence-transformers==5.7.0
transformers==5.15.0
rank-bm25==0.2.2
numpy==2.5.2

pypdf==6.16.1
beautifulsoup4==4.15.0
python-dotenv==1.2.3
python-multipart==0.0.32
