# Backend runtime dependencies, pinned to the versions this project was built
# and tested against locally. Unpinned installs drift; a fresh install six
# months from now would silently pull a different LangChain and break.
#
# torch is NOT listed here on purpose. sentence-transformers pulls it in, and
# the default PyPI wheel bundles CUDA at ~2.5GB. On a machine without an NVIDIA
# GPU, install the CPU-only wheel (~200MB) first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

fastapi==0.141.1
uvicorn==0.52.3
pydantic==2.13.4
starlette==1.6.0

langgraph==1.2.11
langchain==1.3.15
langchain-core==1.5.5
langchain-classic==1.0.8
langchain-groq==1.1.3
groq==0.37.1

psycopg2-binary==2.9.12
pgvector==0.5.0

sentence-transformers==5.7.0
transformers==5.15.0
rank-bm25==0.2.2
numpy==2.5.2

pypdf==6.16.1
beautifulsoup4==4.15.0
python-dotenv==1.2.3
python-multipart==0.0.32

# mcp_server.py only. It used to be omitted here because the deployed API
# process never imported it and the image had a size budget; with no deploy,
# a root install that cannot run the documented MCP server is just broken.
mcp==2.0.0
