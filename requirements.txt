# Versions live in backend/requirements.txt, pinned to what this project was
# built and tested against. Keeping a second, unpinned copy here meant two
# lists that could drift apart; this file defers to the one that is pinned so
# `pip install -r requirements.txt` still works from the project root.

-r backend/requirements.txt
